/**
 * `HomeProps` is an object with a property called `networkList` that is an array of `Network` objects.
 * @property networkList - Network[];
 */
export interface HomeProps {
  networkList: Network[];
}
