import { css } from "@mui/material";

const useStyles = () => ({
  root: css({
    overflow: "visible",
    position: "sticky",
    top: 0,
    width: "100%",
    zIndex: 4,
  }),
});

export default useStyles;
