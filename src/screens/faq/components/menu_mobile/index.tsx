import React from 'react';
import classnames from 'classnames';

const MenuMobile: React.FC<ComponentDefault> = (props) => {
  return (
    <div className={classnames(props.className)}>
      Menu Mobile
    </div>
  );
};

export default MenuMobile;
