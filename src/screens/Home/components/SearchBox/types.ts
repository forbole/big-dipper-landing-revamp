/**
 * `SearchBoxProps` is an object with a property called `networks` that is an array of `Network`
 * objects.
 * @property networks - Network[] - This is the array of networks that we will be searching
 * through.
 */
export interface SearchBoxProps {
  networks: Network[];
}
